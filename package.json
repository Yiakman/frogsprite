{
  "name": "frogsprite",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "frogsprite": "./bin/frogsprite.js"
  },
  "files": [
    "bin",
    "dist",
    "README.md",
    "AGENTS.md"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "prepack": "npm run build",
    "preview": "vite preview",
    "check": "svelte-check --tsconfig ./tsconfig.app.json && tsc -p tsconfig.node.json",
    "test": "node --test 'src/lib/**/*.test.ts'"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^7.1.2",
    "@tsconfig/svelte": "^5.0.8",
    "@types/node": "^24.13.2",
    "svelte": "^5.56.4",
    "svelte-check": "^4.7.1",
    "typescript": "~6.0.2",
    "vite": "^8.1.1"
  }
}